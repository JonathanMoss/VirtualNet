version: '3.8'

services:
  web-app:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
      - ./app:/app/app
      - ./static:/app/static
    environment:
      - FLASK_ENV=development
      - DATABASE_URL=sqlite:////app/data/virtualnet.db
    restart: always
